first {
    fn foo(arg: { bar: int, foo: string }, otherArg: string): void {
        print arg.bar
        print otherArg
    }

    fn main(args: string[]): void {
        let first = { foo = "hi", bar = 45 }
        foo(first, "foo")
    }
}